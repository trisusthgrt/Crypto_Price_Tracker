## Frontend environment variables
#
# If you're using Vite dev proxy (vite.config.ts), you can leave this unset and
# the frontend will call the backend via "/api".
#
# For deployment, set this to your deployed backend base URL.
VITE_API_BASE_URL=http://localhost:5000

# Notifications mode:
# - polling: poll /api/alert-events?unreadOnly=true
# - sse: use /api/stream (still polls count for correctness)
VITE_NOTIFICATIONS_MODE=polling

